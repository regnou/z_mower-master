# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

###############
# Websocket   #
###############
GET 	/create/:tvName 	controllers.Application.create(tvName: String)
GET		/connect/:tvName	controllers.Application.connect(tvName: String)		
