package com.xebia.mower.core;

/**
 * The Enum Direction.<br />
 * Direction (selon la notation cardinale anglaise) dans laquelle se trouve la
 * fameuse tondeuse à gazon automatisée dernier cri.<br />
 * Ne pas modifier l'ordre des valeurs de l'enum.
 */
public enum Orientation {

    /** The N. */
    N,

    /** The E. */
    E,

    /** The S. */
    S,

    /** The W. */
    W;

}
